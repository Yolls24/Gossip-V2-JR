<!-- app/views/comments/_form.html.erb -->
<div class="mb-4">
<%= form_with(model: [gossip, comment], local: true) do |form| %>
    <div class="mb-3">
      <%= form.label :content, "Nouveau commentaire" %>
      <%= form.text_area :content, class: "form-control", rows: 3 %>
    </div>
    <%= form.submit comment.new_record? ? "Ajouter" : "Mettre à jour", class: "btn btn-primary" %>
  <% end %>
  
</div>


