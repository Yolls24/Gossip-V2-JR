<div class="container">
  <h2><%= @gossip.title %></h2>
  <p><%= @gossip.content %></p>
  <p>Auteur : <%= link_to @gossip.user.first_name, user_path(@gossip.user) %></p>
  <p>Date : <%= @gossip.created_at.strftime('%d %B %Y') %></p>
</div>
